<XTMAILING>
    <CAMPAIGN_ID>{{ campaign_id }}</CAMPAIGN_ID>
    {% if 'transaction_id' in fields %}
    <TRANSACTION_ID>{{ fields.transaction_id }}</TRANSACTION_ID>
    {% endif %}
    <SEND_AS_BATCH>false</SEND_AS_BATCH>
	<NO_RETRY_ON_FAILURE>false</NO_RETRY_ON_FAILURE>
    {% if fields %}
    <SAVE_COLUMNS>
        {% for name in fields %}
        <COLUMN_NAME>{{ name }}</COLUMN_NAME>
        {% endfor %}
    </SAVE_COLUMNS>
    {% endif %}
    <RECIPIENT>
        <EMAIL>{{ to }}</EMAIL>
        <BODY_TYPE>HTML</BODY_TYPE>
        {% for key, value in fields.items %}
        <PERSONALIZATION>
            <TAG_NAME>{{ key }}</TAG_NAME>
            <VALUE>{{ value }}</VALUE>
        </PERSONALIZATION>
        {% endfor %}
    </RECIPIENT>
</XTMAILING>
